apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.app.svc.userutilsapi }}
  labels:
    app: {{ template "userutils-api.name" . }}
    chart: {{ template "userutils-api.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "userutils-api.name" . }}
    release: {{ .Release.Name }}
