apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.app.svc.centinelaapigw }}
  labels:
    app: {{ template "apigwcen.name" . }}
    chart: {{ template "apigwcen.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "apigwcen.name" . }}
    release: {{ .Release.Name }}
