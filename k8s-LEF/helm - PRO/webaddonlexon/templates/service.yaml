apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.app.svc.addonlexon }}
  labels:
    app: {{ template "webaddonlexon.name" . }}
    chart: {{ template "webaddonlexon.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "webaddonlexon.name" . }}
    release: {{ .Release.Name }}
