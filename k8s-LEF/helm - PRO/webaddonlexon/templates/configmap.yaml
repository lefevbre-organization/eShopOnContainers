{{- $name := include "webaddonlexon.fullname" . -}}
{{- $identity := include "url-of" (list .Values.app.ingress.entries.identity .) -}}

apiVersion: v1
kind: ConfigMap
metadata:
  name: "cfg-{{ $name }}"
  labels:
    app: {{ template "webaddonlexon.name" . }}
    chart: {{ template "webaddonlexon.chart" .}}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
data:
  all__InstrumentationKey: "{{ .Values.inf.appinsights.key }}"
  all__UseAzureServiceBus: "{{ .Values.inf.eventbus.useAzure }}"
  all_EnableLoadTest: "{{ .Values.inf.misc.useLoadTest }}"
  internalurls__lexonapigw__hc: http://{{ .Values.app.svc.lexonapigw }}/hc
  internalurls__accountapigw__hc: http://{{ .Values.app.svc.accountapigw }}/hc
  config.js: {{ toYaml .Values.configValues | indent 4 }}
