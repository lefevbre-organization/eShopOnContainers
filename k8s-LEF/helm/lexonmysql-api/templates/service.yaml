apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.app.svc.lexonmysqlapi }}
  labels:
    app: {{ template "lexonmysql-api.name" . }}
    chart: {{ template "lexonmysql-api.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "lexonmysql-api.name" . }}
    release: {{ .Release.Name }}
