apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.app.svc.lexon }}
  labels:
    app: {{ template "weblexon.name" . }}
    chart: {{ template "weblexon.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "weblexon.name" . }}
    release: {{ .Release.Name }}
