account: v1
kind: Service
metadata:
  name: {{ .Values.app.svc.account }}
  labels:
    app: {{ template "account-api.name" . }}
    chart: {{ template "account-api.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "account-api.name" . }}
    release: {{ .Release.Name }}
