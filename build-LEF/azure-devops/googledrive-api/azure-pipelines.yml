variables:
    registryEndpoint: lefebvre-docker
trigger:
  branches:
    include:
    - lef-main-containers
  paths:
    include:
    - src/BuildingBlocks/*
    - src/Services/Google/GoogleDrive.API/*
    - deploy/k8s-lef/helm/googledrive-api/*
jobs:
- template: ../buildimages.yaml
  parameters:
    services: googledrive.api
    registryEndpoint: $(registryEndpoint)
    helmfrom: $(Build.SourcesDirectory)/k8s-LEF/helm
    helmto: $(Build.ArtifactStagingDirectory)/k8s-LEF/helm    

