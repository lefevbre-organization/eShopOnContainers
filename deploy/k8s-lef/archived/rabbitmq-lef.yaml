apiVersion: v1
kind: Service
metadata:
  labels:
    app: elefebvre
    component: rabbitmq
  name: rabbitmq
spec:
  ports:
  - port: 5672
  selector:
    app: elefebvre
    component: rabbitmq
---
apiVersion: extensions/v1
kind: Deployment
metadata:
  name: rabbitmq
spec:
  template:
    metadata:
      labels:
        app: elefebvre
        component: rabbitmq
    spec:
      containers:
      - name: rabbitmq
        image: rabbitmq:3-management
        ports:
        - containerPort: 5672
