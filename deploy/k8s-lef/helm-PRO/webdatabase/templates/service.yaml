apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.app.svc.database }}
  labels:
    app: {{ template "webdatabase.name" . }}
    chart: {{ template "webdatabase.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "webdatabase.name" . }}
    release: {{ .Release.Name }}
