apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.app.svc.signatureapi }}
  labels:
    app: {{ template "signature-api.name" . }}
    chart: {{ template "signature-api.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "signature-api.name" . }}
    release: {{ .Release.Name }}
