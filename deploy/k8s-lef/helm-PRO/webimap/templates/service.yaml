apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.app.svc.imap }}
  labels:
    app: {{ template "webimap.name" . }}
    chart: {{ template "webimap.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "webimap.name" . }}
    release: {{ .Release.Name }}
