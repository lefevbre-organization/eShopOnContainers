apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-%s" .Release.Name "mariadb-master"  }}
  namespace: default
  labels:
    app: "mariadb"
    component: "master"
    chart: "mariadb-7.10.2"
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: ClusterIP
  ports:
    - name: mysql
      port: 3306
      targetPort: mysql
  selector:
    app: "mariadb"
    component: "master"
    release: {{ .Release.Name }}