apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.app.svc.signatureapigw }}
  labels:
    app: {{ template "apigwsig.name" . }}
    chart: {{ template "apigwsig.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    env: {{ .Values.lef.env }}
    version: {{ .Chart.Version | quote }} 
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "apigwsig.name" . }}
    release: {{ .Release.Name }}
