{{- $name := include "apigwsig.fullname" . -}}

apiVersion: v1
kind: ConfigMap
metadata:
  name: "ocelot-{{ $name }}"
  labels:
    app: {{ template "apigwsig.name" . }}
    chart: {{ template "apigwsig.chart" .}}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    env: {{ .Values.lef.env }}
    version: {{ .Chart.Version | quote }}
data:
  {{ (.Files.Glob "configuration-signature.json").AsConfig  | indent 2 }}

