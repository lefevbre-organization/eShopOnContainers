apigwlex: v1
kind: Service
metadata:
  name: {{ .Values.app.svc.webaccountapigw }}
  labels:
    app: {{ template "apigwlex.name" . }}
    chart: {{ template "apigwlex.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "apigwlex.name" . }}
    release: {{ .Release.Name }}
