apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.app.svc.portal }}
  labels:
    app: {{ template "webportal.name" . }}
    chart: {{ template "webportal.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "webportal.name" . }}
    release: {{ .Release.Name }}
