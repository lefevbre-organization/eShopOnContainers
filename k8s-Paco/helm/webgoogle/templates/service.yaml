apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.app.svc.google }}
  labels:
    app: {{ template "webgoogle.name" . }}
    chart: {{ template "webgoogle.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "webgoogle.name" . }}
    release: {{ .Release.Name }}
