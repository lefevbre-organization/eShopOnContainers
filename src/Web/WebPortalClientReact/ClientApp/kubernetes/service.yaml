apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: service
  name: webportalclient
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: webportalclient
  type: NodePort
