apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: service
  name: weblexonclient
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: weblexonclient
  type: NodePort
